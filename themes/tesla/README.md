# Tesla Theme

Tesla-inspired minimal three-column layout: TOC on the left, content in the middle, post meta and knowledge graph on the right. The visual language uses white surfaces, carbon text, electric-blue actions, flat chrome, and tight rectangular controls.

Supports both light and dark modes via `prefers-color-scheme` and a manual override stored in localStorage.
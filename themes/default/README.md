# Default Theme

Notion-inspired three-column layout: TOC on the left, content in the middle, post meta and knowledge graph on the right.

Supports both light and dark modes via `prefers-color-scheme` and a manual override stored in localStorage.
